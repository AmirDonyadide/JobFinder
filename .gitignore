# This orphan branch publishes only the static website. Ignore everything by
# default so files left in the worktree after switching from `main` cannot be
# committed or deployed accidentally.
/*

!/.gitignore
!/.nojekyll
!/README.md
!/*.html

!/css/
!/css/**

!/js/
!/js/**

!/assets/
!/assets/**
